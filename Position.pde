/*static abstract class PositionStatic {int x
}

public class Position extends PositionStatic {
  private int x;
  private int y;
  public Position(int x, int y) {
    this.x = x;
    this.y = y;
  }
  // this + argument
  public Position Add(Position pos) {
    int x = this.x + pos.x;
    int y = this.y + pos.y;
    return new Position(x, y);
  }
  // this - argument
  public Position Sub(Position pos) {
    int x = this.x - pos.x;
    int y = this.y - pos.y;
    return new Position(x, y);
  }
}*/
  
  
  